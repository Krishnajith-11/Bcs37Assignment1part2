apiVersion: apps/v1
kind: Deployment
metadata:
  name: churn-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: churn
  template:
    metadata:
      labels:
        app: churn
    spec:
      containers:
      - name: churn
        image: churn-service:latest
        ports:
        - containerPort: 8000